{
  "name": "@openclaw/capsule-claw-guard",
  "version": "2026.2.2",
  "description": "Security guard plugin - logs tool calls and uses LLM to detect security risks before execution",
  "type": "module",
  "devDependencies": {
    "openclaw": "workspace:*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
